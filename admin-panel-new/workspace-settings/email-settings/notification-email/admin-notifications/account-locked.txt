1.
Title:
should uncheck account locked notification checkbox

Steps:
# Login to the workspace as workspace admin
# Go to the manage workspace (new) >> Email Settings >> Notification Email page
# Navigate to Admin Notifications section
# Find "Account Locked" notification setting
# Uncheck the notification checkbox

Expected Result:
# Should uncheck account locked email notifications
# Checkbox should be unmarked

2.
Title:
should check account locked notification checkbox

Steps:
# Login to the workspace as workspace admin
# Go to the manage workspace (new) >> Email Settings >> Notification Email page
# Navigate to Admin Notifications section
# Find "Account Locked" notification setting
# Check the notification checkbox

Expected Result:
# Should check account locked email notifications
# Checkbox should be marked as checked

3.
Title:
should save account locked notification settings

Steps:
# Login to the workspace as workspace admin
# Go to the manage workspace (new) >> Email Settings >> Notification Email page
# Navigate to Admin Notifications section
# Enable/disable account locked notification checkbox
# Click save button

Expected Result:
# Should save the account locked notification settings
# Should display success confirmation message

4.
Title:
should set sent notification before number

Steps:
# Login to the workspace as workspace admin
# Go to the manage workspace (new) >> Email Settings >> Notification Email page
# Navigate to Admin Notifications section
# Check the "account locked" chekbox
# Find "Sent Notification Before" field
# Enter valid value

Expected Result:
# Should set the sent notification before number
# Field should accept valid numeric input

4.
Title:
should not accept invalid input for sent notification before

Steps:
# Login to the workspace as workspace admin
# Go to the manage workspace (new) >> Email Settings >> Notification Email page
# Navigate to Admin Notifications section
# Check the "community seat limit reached" checkbox
# Find "Seat count lower threshold" field
# Enter invalid input (non-numeric or negative values or long numeric value)

Expected Result:
# Should not accept invalid input
# Should display field error message